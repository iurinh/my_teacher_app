<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.iuri.my_teacher_app"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="14" />

    <uses-permission android:name="android.permission.SEND_EMAIL" />
    <uses-permission android:name="android.permission.WRITE_EMAIL" />
    
    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login" >
             <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".NewLoginActivity"
            android:label="@string/title_activity_new_login" >
        </activity>
        <activity
            android:name=".ConfirmCodeAccessActivity"
            android:label="@string/title_activity_confirm_code_access" >
        </activity>
    </application>

</manifest>
